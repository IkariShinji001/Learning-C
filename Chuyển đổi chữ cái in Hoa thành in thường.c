Viết chương trình in cho phép nhập vào 1 chữ cái HOA, in ra màn hình chữ in thường của nó.

===================================================================================================================================================================================

#include <stdio.h>

int main() {
    char Char;
    scanf("%c", &Char);
    printf("%c", Char + 32);
    return 0;
}
